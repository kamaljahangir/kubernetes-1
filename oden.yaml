apiVersion: v1
kind: LimitRange
metadata:
        name: shree
spec:
        limits:
                - default:
                        memory: 512Mi
                  defaultRequest:
                        memory: 256Mi
                  type: Container
                        
                        
